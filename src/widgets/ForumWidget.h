#pragma once
#include <QWidget>

class ForumWidget : public QWidget
{
    Q_OBJECT

public:
    explicit ForumWidget(QWidget* parent = nullptr);
    ~ForumWidget();

public slots:


protected:

private:

};