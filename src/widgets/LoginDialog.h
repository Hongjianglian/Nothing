#pragma once

#include <QWidget>

/// <summary>
/// 登录对话框
/// </summary>
class LoginDialog: public QWidget
{
    Q_OBJECT

public:
    explicit LoginDialog(QWidget* parent = NULL);
    ~LoginDialog();

protected:

private:

};